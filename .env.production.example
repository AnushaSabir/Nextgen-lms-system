# ── GrapeTask LMS Frontend — production env (Vercel) ─────────────────────────
# Point at the deployed backend's REST API. MUST end with /api (no trailing slash after).
# e.g. https://your-backend.up.railway.app/api
NEXT_PUBLIC_API_URL=https://your-backend.up.railway.app/api
